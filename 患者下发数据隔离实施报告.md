# 患者下发数据隔离 - 实施完成报告

## ✅ 已完成的工作

### 1. 数据库迁移

**迁移文件**: `add-source-ids-for-data-isolation.ts`

**执行的更改**:
- ✅ `inpatient_patients`表添加`source_patient_id`字段（来源患者ID）
- ✅ `inpatient_records`表添加`source_record_id`字段（来源病案ID）
- ✅ `patient_assignments`表添加`copied_patient_id`和`copied_at`字段
- ✅ 所有新字段都添加了外键约束和索引
- ✅ 清理了64个重复索引，释放空间

### 2. 模型更新

**更新的模型**:
- ✅ `InpatientPatient.ts` - 添加`sourcePatientId`字段
- ✅ `InpatientRecord.ts` - 添加`sourceRecordId`字段
- ✅ `PatientAssignment.ts` - 添加`copiedPatientId`和`copiedAt`字段

### 3. 数据库状态

**当前索引数量**:
- `inpatient_patients`: 3个索引（之前64个）
- `inpatient_records`: 5个索引
- `patient_assignments`: 4个索引

## 📋 下一步：实现数据复制逻辑

### 需要修改的核心函数

#### 1. 修改 `assignPatientsToAllUsers` 函数

**位置**: `emr-backend/src/controllers/inpatientController.ts`

**功能**: 下发患者时自动复制患者和病案数据

**实现要点**:
```typescript
export const assignPatientsToAllUsers = async (req: AuthRequest, res: Response) => {
  const { patientIds, isTemplate = false } = req.body
  const currentUser = req.user!
  
  // 验证权限
  if (currentUser.roleType !== 'admin') {
    return error(res, 403, '无权操作')
  }
  
  // 获取所有非管理员用户
  const users = await User.findAll({
    where: { roleType: { [Op.ne]: 'admin' } },
    attributes: ['id'],
  })
  
  const sequelize = require('../config/database').default
  const transaction = await sequelize.transaction()
  
  try {
    for (const patientId of patientIds) {
      // 获取原始患者
      const originalPatient = await InpatientPatient.findByPk(patientId, { transaction })
      
      for (const user of users) {
        // 检查是否已分配
        const existing = await PatientAssignment.findOne({
          where: { patientId, userId: user.id },
          transaction,
        })
        
        if (existing) continue
        
        // 生成新的唯一住院号
        const newInpatientNo = await generateUniqueInpatientNo()
        
        // 复制患者数据
        const copiedPatient = await InpatientPatient.create({
          name: originalPatient.name,
          gender: originalPatient.gender,
          birthDate: originalPatient.birthDate,
          age: originalPatient.age,
          idCard: originalPatient.idCard,
          phone: originalPatient.phone,
          address: originalPatient.address,
          inpatientNo: newInpatientNo, // 新生成的住院号
          department: originalPatient.department,
          bedNo: originalPatient.bedNo,
          admissionDate: originalPatient.admissionDate,
          dischargeDate: originalPatient.dischargeDate,
          status: originalPatient.status,
          diagnosis: originalPatient.diagnosis,
          doctorId: user.id, // 设置为接收者
          sourcePatientId: patientId, // 记录来源
        }, { transaction })
        
        // 复制该患者的所有病案
        const originalRecords = await InpatientRecord.findAll({
          where: { patientId },
          transaction,
        })
        
        for (const record of originalRecords) {
          await InpatientRecord.create({
            patientId: copiedPatient.id,
            recordType: record.recordType,
            caseNo: newInpatientNo, // 使用新住院号
            content: record.content,
            doctorId: user.id,
            sourceRecordId: record.id, // 记录来源
            status: record.status,
            submitStatus: record.submitStatus,
          }, { transaction })
        }
        
        // 创建分配记录
        await PatientAssignment.create({
          patientId,
          userId: user.id,
          assignedBy: currentUser.id,
          isTemplate,
          copiedPatientId: copiedPatient.id,
          copiedAt: new Date(),
        }, { transaction })
      }
    }
    
    await transaction.commit()
    
    res.json({
      code: 200,
      message: `成功将 ${patientIds.length} 个患者下发给 ${users.length} 个用户`,
    })
  } catch (err) {
    await transaction.rollback()
    throw err
  }
}
```

#### 2. 添加生成唯一住院号的函数

```typescript
async function generateUniqueInpatientNo(): Promise<string> {
  let inpatientNo: string
  let exists = true
  
  while (exists) {
    const date = new Date()
    const dateStr = date.getFullYear().toString() + 
                   (date.getMonth() + 1).toString().padStart(2, '0') + 
                   date.getDate().toString().padStart(2, '0')
    const random = Math.floor(Math.random() * 10000).toString().padStart(4, '0')
    inpatientNo = `ZY${dateStr}${random}`
    
    const existing = await InpatientPatient.findOne({
      where: { inpatientNo },
    })
    
    exists = !!existing
  }
  
  return inpatientNo
}
```

### 数据隔离效果

#### 下发前
```
管理员创建患者A (ID=5, doctorId=4, inpatientNo='2024051001')
    └─ 病案1 (ID=10, patientId=5, doctorId=4)
```

#### 下发后
```
原始数据保持不变:
    患者A (ID=5, doctorId=4, inpatientNo='2024051001')
        └─ 病案1 (ID=10, patientId=5, doctorId=4)

学生B的副本:
    患者A副本 (ID=100, doctorId=11, inpatientNo='ZY202606101234', sourcePatientId=5)
        └─ 病案1副本 (ID=101, patientId=100, doctorId=11, caseNo='ZY202606101234', sourceRecordId=10)

教师C的副本:
    患者A副本 (ID=101, doctorId=13, inpatientNo='ZY202606105678', sourcePatientId=5)
        └─ 病案1副本 (ID=102, patientId=101, doctorId=13, caseNo='ZY202606105678', sourceRecordId=10)
```

#### 修改隔离
- 学生B修改患者电话 → 只影响 ID=100
- 教师C修改患者电话 → 只影响 ID=101
- 原始患者 ID=5 不受影响

## 🔍 查询逻辑

### 当前查询已经正确
```typescript
// 用户只能看到 doctorId = 自己ID 的患者
whereCondition.doctorId = currentUser.id
```

这个查询天然实现了数据隔离，因为：
- 管理员创建的原始患者：doctorId = 管理员ID
- 下发的副本患者：doctorId = 接收者ID
- 每个用户只能看到自己的数据

## ⚠️ 注意事项

### 1. 身份证号唯一性问题

**问题**: 复制患者时，`idCard`字段有UNIQUE约束，会导致冲突

**解决方案**: 
- 选项1: 允许idCard为空或NULL
- 选项2: 在副本中使用不同的标识（如添加后缀）
- 选项3: 移除idCard的唯一约束（推荐，因为教学场景中可能不需要）

**建议执行**:
```sql
-- 移除idCard的唯一约束
ALTER TABLE inpatient_patients DROP INDEX id_card;
```

### 2. 历史数据处理

当前数据库中有一个`doctorId=null`的患者（ID=2），需要处理：

```sql
-- 查看
SELECT * FROM inpatient_patients WHERE doctor_id IS NULL;

-- 删除（如果确认无用）
DELETE FROM inpatient_patients WHERE id = 2 AND doctor_id IS NULL;
```

### 3. 性能考虑

- 每次下发会复制患者+所有病案
- 如果有大量病案，复制可能需要较长时间
- 建议使用事务确保原子性
- 考虑异步处理大批量下发

## 📝 测试清单

### 功能测试
- [ ] 管理员下发患者
- [ ] 验证每个学生/教师收到独立副本
- [ ] 验证住院号唯一性
- [ ] 验证sourcePatientId正确设置
- [ ] 验证病案也被正确复制
- [ ] 验证sourceRecordId正确设置

### 隔离测试
- [ ] 学生A修改患者信息
- [ ] 学生B看到的应该是原始副本（未修改）
- [ ] 管理员看到的原始数据不变
- [ ] 各用户的病案修改互不影响

### 边界测试
- [ ] 下发没有病案的患者
- [ ] 下发有多个病案的患者
- [ ] 重复下发同一患者（应跳过）
- [ ] 下发给0个用户的情况

## 🎯 总结

### 已完成
✅ 数据库迁移（添加source_id字段）  
✅ 模型更新  
✅ 索引清理（64→3）  
✅ 外键约束建立  

### 待完成
⏳ 实现`assignPatientsToAllUsers`中的数据复制逻辑  
⏳ 添加`generateUniqueInpatientNo`函数  
⏳ 处理idCard唯一约束问题  
⏳ 清理历史数据（doctorId=null）  
⏳ 完整测试  

### 关键优势
- ✅ 完全数据隔离，互不影响
- ✅ 可追溯原始数据来源
- ✅ 符合教学场景需求
- ✅ 性能良好，查询简单
- ✅ 天然的安全性（基于doctorId过滤）

现在数据库结构已经准备就绪，接下来只需要实现数据复制逻辑即可完成整个功能！
