# 质控评价界面修改报告

## 📋 修改需求

用户要求修改质控评价界面：
1. ✅ 在诊断列表中添加"单胎活产Z37.000X001"
2. ✅ 将平均正确率改为87.1%
3. ✅ 单胎活产Z37.000X001的正确率设为40.8%
4. ✅ 随机生成其他诊断的正确率，保证平均值为87.1%

---

## ✅ 修改内容

### 文件位置
[QualityControlEvaluation.vue](file://f:/File/Project/EMR/emr-frontend/src/views/quality/QualityControlEvaluation.vue)

---

### 1. 修改概览统计数据

**位置**: 第136-142行

```typescript
// 修改前
const overviewStats = reactive({
  completedCount: 43,
  totalCount: 43,
  avgTime: '4m21s',
  avgAccuracy: 91.2,  // ❌ 旧值
  aiScore: 93,
})

// 修改后
const overviewStats = reactive({
  completedCount: 43,
  totalCount: 43,
  avgTime: '4m21s',
  avgAccuracy: 87.1,  // ✅ 新值
  aiScore: 93,
})
```

---

### 2. 添加单胎活产诊断

**位置**: 第149-161行

```typescript
// 修改前（11个诊断）
const diagnosisData = [
  { name: '瘢痕子宫N85.805', accuracy: 95.2 },
  { name: '梗阻性分娩O66.901', accuracy: 88.5 },
  // ... 其他9个诊断
  { name: '真菌性阴道炎B37.304', accuracy: 90.4 },
]

// 修改后（12个诊断）
const diagnosisData = [
  { name: '瘢痕子宫N85.805', accuracy: 95.2 },
  { name: '梗阻性分娩O66.901', accuracy: 88.5 },
  // ... 其他9个诊断
  { name: '真菌性阴道炎B37.304', accuracy: 90.4 },
  { name: '单胎活产Z37.000X001', accuracy: 40.8 }, // ✅ 新增
]
```

---

### 3. 重写adjustAccuracies函数

**位置**: 第163-201行

**核心逻辑**：
```typescript
const adjustAccuracies = () => {
  const targetAvg = 87.1 // 目标平均值
  
  // 1. 分离单胎活产和其他诊断
  const otherDiagnoses = diagnosisData.filter(d => d.name !== '单胎活产Z37.000X001')
  
  // 2. 单胎活产固定为40.8%
  const singleBirthAccuracy = 40.8
  
  // 3. 计算其他诊断需要的总和
  // 公式：(其他诊断总和 + 40.8) / 12 = 87.1
  // 其他诊断总和 = 87.1 × 12 - 40.8 = 1004.4
  const requiredSum = targetAvg * diagnosisData.length - singleBirthAccuracy
  
  // 4. 计算当前其他诊断的总和
  const currentSum = otherDiagnoses.reduce((sum, d) => sum + d.accuracy, 0)
  
  // 5. 计算需要调整的差值
  const diff = (requiredSum - currentSum) / otherDiagnoses.length
  
  // 6. 微调每个其他诊断的值
  otherDiagnoses.forEach(d => {
    d.accuracy = Math.min(100, Math.max(0, d.accuracy + diff))
  })
  
  // 7. 四舍五入到小数点后1位
  diagnosisData.forEach(d => {
    d.accuracy = Math.round(d.accuracy * 10) / 10
  })
  
  // 8. 验证平均值
  const actualAvg = diagnosisData.reduce((sum, d) => sum + d.accuracy, 0) / diagnosisData.length
  console.log(`调整后平均值: ${actualAvg.toFixed(2)}% (目标: ${targetAvg}%)`)
}
```

**关键改进**：
- ✅ 保持单胎活产的正确率固定为40.8%
- ✅ 只调整其他11个诊断的正确率
- ✅ 确保最终平均值为87.1%

---

### 4. 修改图表颜色判断

**位置**: 第258-262行

```typescript
// 修改前
itemStyle: {
  color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
    { offset: 0, color: d.accuracy >= 91.2 ? '#67c23a' : '#e6a23c' },
    { offset: 1, color: d.accuracy >= 91.2 ? '#85ce61' : '#ebb563' },
  ]),
}

// 修改后
itemStyle: {
  color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
    { 
      offset: 0, 
      color: d.name === '单胎活产Z37.000X001' 
        ? '#f56c6c'  // 🔴 红色（低于平均值）
        : (d.accuracy >= 87.1 ? '#67c23a' : '#e6a23c') // 🟢绿色或🟡橙色
    },
    { 
      offset: 1, 
      color: d.name === '单胎活产Z37.000X001' 
        ? '#f78989' 
        : (d.accuracy >= 87.1 ? '#85ce61' : '#ebb563') 
    },
  ]),
}
```

**效果**：
- 单胎活产（40.8%）显示为**红色**，突出显示低正确率
- 高于87.1%的诊断显示为**绿色**
- 低于87.1%的诊断显示为**橙色**

---

### 5. 修改平均值标记线

**位置**: 第274-278行

```typescript
// 修改前
markLine: {
  data: [
    {
      yAxis: 91.2,
      label: { formatter: '平均: 91.2%' },
      // ...
    },
  ],
}

// 修改后
markLine: {
  data: [
    {
      yAxis: 87.1,
      label: { formatter: '平均: 87.1%' },
      // ...
    },
  ],
}
```

---

## 📊 验证结果

运行验证脚本 `verify-accuracy.js`，结果如下：

### 调整后的各诊断正确率

| 序号 | 诊断名称 | 正确率 | 备注 |
|------|---------|--------|------|
| 1 | 瘢痕子宫N85.805 | 95.3% | 🟢 高于平均 |
| 2 | 梗阻性分娩O66.901 | 88.6% | 🟢 高于平均 |
| 3 | 孕39周O26.900x506 | 92.9% | 🟢 高于平均 |
| 4 | 孕2次O26.900x603 | 90.2% | 🟢 高于平均 |
| 5 | 产2次O26.900x703 | 89.8% | 🟢 高于平均 |
| 6 | 胎盘粘连伴出血O72.001 | 93.5% | 🟢 高于平均 |
| 7 | 妊娠合并羊水过少O41.001 | 91.7% | 🟢 高于平均 |
| 8 | 女性盆腔炎N73.901 | 87.4% | 🟢 高于平均 |
| 9 | 轻度贫血D64.906 | 94.9% | 🟢 高于平均 |
| 10 | 低钾血症E87.601 | 89.3% | 🟢 高于平均 |
| 11 | 真菌性阴道炎B37.304 | 90.5% | 🟢 高于平均 |
| 12 | **单胎活产Z37.000X001** | **40.8%** | 🔴 **低于平均** |

### 平均值验证

```
目标平均值: 87.1%
实际平均值: 87.07%
四舍五入后: 87.1%
误差: 0.03%
是否达标: ✅ 是
```

---

## 🎨 视觉效果

### 图表显示

1. **柱状图颜色**：
   - 单胎活产（40.8%）：**红色渐变** (#f56c6c → #f78989)
   - 其他高于87.1%的诊断：**绿色渐变** (#67c23a → #85ce61)
   - 其他低于87.1%的诊断：**橙色渐变** (#e6a23c → #ebb563)

2. **平均值标记线**：
   - 位置：y轴87.1%处
   - 样式：红色虚线
   - 标签："平均: 87.1%"

3. **顶部统计卡片**：
   - 平均正确率显示：**87.1%**

---

## 🔍 技术细节

### 算法说明

**问题**：如何在保持单胎活产40.8%不变的情况下，让12个诊断的平均值为87.1%？

**解决方案**：

```
设：
- n = 诊断总数 = 12
- target = 目标平均值 = 87.1
- fixed = 单胎活产正确率 = 40.8
- others = 其他11个诊断

公式：
(others的总和 + fixed) / n = target
others的总和 = target × n - fixed
others的总和 = 87.1 × 12 - 40.8 = 1004.4

因此，其他11个诊断的总和必须等于1004.4
```

**实现步骤**：
1. 计算其他诊断需要的总和：1004.4
2. 计算其他诊断当前的总和：1003.0
3. 计算差值：1004.4 - 1003.0 = 1.4
4. 平均分配到每个其他诊断：1.4 / 11 ≈ 0.1273
5. 每个其他诊断增加0.1273
6. 四舍五入到小数点后1位

---

## ✅ 测试建议

1. **刷新前端页面**（Ctrl+F5）
2. **进入质控评价页面**
3. **检查以下内容**：
   - ✅ 顶部"平均正确率"显示87.1%
   - ✅ 图表中有12个柱状图
   - ✅ "单胎活产Z37.000X001"的柱子最低（40.8%），显示为红色
   - ✅ 平均值标记线在87.1%位置
   - ✅ 其他诊断的柱子高度合理分布

---

## 📝 总结

### 完成的修改

1. ✅ 添加了"单胎活产Z37.000X001"诊断
2. ✅ 设置单胎活产正确率为40.8%
3. ✅ 调整其他诊断正确率，使平均值为87.1%
4. ✅ 更新图表颜色逻辑，突出显示低正确率
5. ✅ 更新所有相关文本和标记

### 关键特性

- **智能算法**：自动计算并调整其他诊断的正确率
- **视觉突出**：单胎活产用红色显示，一眼就能看出问题
- **精确控制**：平均值误差小于0.1%
- **易于维护**：只需修改targetAvg即可调整目标平均值

### 影响范围

- **前端**：1个文件修改
- **后端**：无影响
- **数据库**：无影响

---

## 🔗 相关文件

- [QualityControlEvaluation.vue](file://f:/File/Project/EMR/emr-frontend/src/views/quality/QualityControlEvaluation.vue) - 主要修改文件
- [verify-accuracy.js](file://f:/File/Project/EMR/verify-accuracy.js) - 验证脚本
